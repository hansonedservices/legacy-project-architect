services:
  - type: web
    name: legacy-project-architect
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: GEMINI_API_KEY
        sync: false
      - key: DB_HOST
        value: db.bawwsxzwlcwrbtrklnzl.supabase.co
      - key: DB_PASSWORD
        sync: false
